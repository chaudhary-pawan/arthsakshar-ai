import asyncio
import sarvam_ai

async def test():
    # Use a dummy audio or an existing recording URL if I had one
    # I'll just test the base API with missing file to see the URL/auth error, 
    # or create a tiny wav file.
    pass

if __name__ == "__main__":
    asyncio.run(test())
